# Step 4F · Full Repository Document Inheritance Start

Date: 2026-08-16  
Repository: `Th23144/spatial-flow-v2-preview-lab`

## Trigger

The user explicitly required the current Project-2 work window to inherit the project comprehensively by reading the repository's explanatory/project-state Markdown documents in full, including historical implementation, rejection, rollback, acceptance, correction, ownership, deferred-plan, and policy records.

The user also re-emphasized the standing rule that every meaningful project action must be recorded in GitHub before advancing.

## Scope of this inheritance pass

The pass covers:

```text
root-level Project-2 policy/state Markdown
project2-progress/*.md
historical implementation records
failure/rejection/rollback records
source/runtime acceptance records
Checkout Step 4F lineage
Cart Step 4E lineage
Product Packaging lineage
Crypto/payment lineage
deferred/backlog records
cross-window ownership records
```

`project2-progress/rollback/` and `project2-progress/snippets/` were inspected as directory scope; they contain code/CSS artifacts rather than Markdown explanatory records, so they are not counted as Markdown documents in this reading pass.

## Reading rule

This is not a title-only or summary-only scan. The intent is to read document bodies and inherit both final decisions and the reasoning/history that produced them.

Conflicting historical state is resolved with this precedence:

```text
1. later explicit correction / rejection / rollback
2. later formal runtime or source acceptance / closure
3. standing policy unless explicitly superseded
4. older progress/state snapshots as historical evidence only
```

Therefore an older `CURRENT_STATE`, docs index, page matrix, candidate, or plan must not overwrite a later formal acceptance or correction.

## Current authoritative task boundary before continuing

The immediate rollback instruction remains narrowly scoped:

```text
Undo the complete Selected-Payment theme delta added in the immediately preceding manual theme-integration step.
Do not roll back Spatial Flow Crypto Pay Trial V0.3.0.
Keep Project 2 Crypto Workspace OFF until BIG BATCH B runtime acceptance begins.
```

The four theme files must return to the already accepted pre-change identities documented in:

`project2-progress/STEP_4F_BIG_BATCH_B_THEME_POST_INSTALL_AUDIT_20260816.md`.

## Additional user directive to formalize after inheritance

Future cross-window handoffs must default to a **self-contained standalone handoff document delivered directly to the user for forwarding**. A dedicated subsystem window, especially the payment-development window, must not be required to broadly read the Project-2 repository simply to resume its work. Repository paths/commits remain Project-2 audit evidence, not the default external-window intake interface.

## Status

```text
Full repository Markdown inheritance: IN PROGRESS
Standalone external-window handoff policy update: PENDING AFTER INHERITANCE PASS
Standalone payment-window handoff artifact: PENDING AFTER INHERITANCE PASS
Theme Selected-Payment rollback: NOT YET VALIDATED IN THIS WINDOW
V0.3.0 Crypto plugin: RETAIN
Project 2 Crypto Workspace: OFF
```

No implementation step may advance past the current rollback gate until the inheritance pass is closed and the user's four restored theme files are validated.
