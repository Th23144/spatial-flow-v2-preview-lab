# Step 4E-B2-R5-E6-A · Final Live Screenshot Capture Spec

Last updated: 2026-07-21  
Repository: `Th23144/spatial-flow-v2-preview-lab`

## Responsibility boundary

The approved static Cart reference is already stored in GitHub:

```text
preview/spatial-flow-cart-v1.html
```

The assistant is responsible for reading, rendering and inspecting the static reference at all required viewport sizes.

The user must not recreate, capture or upload static-reference screenshots.

The user provides only the three live local-site screenshots because `http://spatialflow.local/cart-2/` is not accessible from the assistant runtime.

## Required live screenshots

```text
A2-live-cart-desktop-1366x768.png
Viewport: 1366 × 768 CSS px

A4-live-cart-mobile-390x844.png
Viewport: 390 × 844 CSS px

A6-live-cart-mobile-360x800.png
Viewport: 360 × 800 CSS px
```

## Required capture method

For each screenshot:

```text
1. Browser page zoom: 100%.
2. DevTools responsive viewport set to the exact width and height above.
3. DevTools preview scale may be Fit, 50%, 75% or any convenient value; it does not affect exported evidence.
4. Reload the Cart after setting the viewport.
5. Keep the same Cart item and quantity state across A2, A4 and A6.
6. Keep menu, Coupon, address editor, Undo notice and validation states closed.
7. Do not run any temporary Console style.
8. Use DevTools command: Capture full size screenshot.
9. Upload the downloaded PNG directly without recropping, resizing or recompressing.
```

## Full-page evidence requirements

Every image must include the complete page flow:

```text
- Header
- breadcrumb
- Your Bag heading and count
- complete product area
- complete Order Summary
- complete recommendation section
- complete Footer
```

The screenshots must not include:

```text
- browser tabs or address bar
- DevTools panels
- inspector highlights
- red rectangles
- open overlays or menus
- stale temporary test styles
```

## Static evidence generated by assistant

The assistant independently generates:

```text
A1-static-cart-desktop-1366x768.png
A3-static-cart-mobile-390x844.png
A5-static-cart-mobile-360x800.png
```

These static files are not requested from the user.

## Current gate

```text
Awaiting A2, A4 and A6 only.
No source edit authorized.
Cart status: Not done.
```
