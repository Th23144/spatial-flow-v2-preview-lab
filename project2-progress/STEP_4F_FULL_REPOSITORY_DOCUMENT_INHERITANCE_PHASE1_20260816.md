# Step 4F · Full Repository Document Inheritance — Phase 1

Date: 2026-08-16  
Repository: `Th23144/spatial-flow-v2-preview-lab`

## Status

```text
Full repository Markdown inheritance: IN PROGRESS
Phase 1: COMPLETE
Implementation changes: NONE
Current live-code gate: unchanged
```

## Phase-1 body-reading coverage

The current window has read the full bodies, not only titles/summaries, for the following groups:

```text
Root Project-2 policy/state documentation
Project-2 progress index / log / deferred-plan documentation
Step 4C Shop rework/audit lineage
Step 4D Single Product cleanup/sync lineage
Step 4E Cart audit and B0/B1 lineage
Step 4E Cart B2 deletion failure / exact rollback lineage
Step 4E Cart R1/R2 consolidation lineage
Step 4E Cart mobile correction / functional-regression lineage
Step 4E Cart R5-A/R5-B canonical ownership lineage
Step 4E Cart R5-C staged legacy deletion lineage
Step 4E Cart R5-D2 backend-editability / dynamic-ownership validation
Step 4E Cart R5-D3/D4 final-inspection failure and reopen trigger
```

The inheritance pass is continuing through the remaining R5-E/R5-N records and the complete Step-4F Checkout / Packaging / Crypto lineage before it can be closed.

## High-value rules inherited and confirmed

### 1. GitHub advancement rule

`PROJECT2_STEP_RECORDING_POLICY.md` is authoritative:

> Every meaningful project step must be recorded in the GitHub repository before advancing to the next step.

The user re-emphasized this on 2026-08-16. This window is following it by recording the inheritance pass itself before implementation advances.

### 2. Evidence outranks expectation

Historical examples repeatedly prove that predicted metrics or planned behavior do not override observed evidence. Examples include:

```text
unexpected but normalized version-only PHP drift accepted after exact proof
source-exact Cart fixes rejected when browser evidence disproved the hypothesis
CSS deletions rejected despite exact SHA/parser success when browser structure collapsed
whitespace-only metric deviations accepted after byte-level explanation
```

### 3. Source correctness is not visual acceptance

A PHP/CSS/JS file can be exactly correct relative to an instruction and the step can still fail at runtime or strict visual review.

### 4. Functional acceptance is not strict 1:1 acceptance

Cart native functions passed before the page passed strict visual review. Later final D3/D4 inspection reopened the task even after canonical CSS cleanup and backend-editability checks.

### 5. No append-only CSS maintenance

The Cart history is the clearest proof: replacement ownership must be established, validated, and then legacy layers removed in bounded reversible batches. Large deletion-before-replacement caused a severe collapse and was rolled back byte-identically.

### 6. Backend editability remains a hard architectural boundary

Frontend hooks may be disabled while renderers/settings are retained for rollback/future use. Dynamic product/cart/recommendation truth remains WooCommerce/backend owned; fake frontend product data is rejected.

### 7. Diagnose live ownership before adding another patch

The mobile Cart remove-icon history established that a post-load visual defect must be diagnosed against the final DOM/cascade before further overrides are added. The Astra-injected SVG/grid-track conflict was solved by correcting ownership, not by continuing font/color guessing.

### 8. Historical state snapshots do not override later closures/corrections

Older `CURRENT_STATE`, docs-index or page-matrix files remain historical evidence. Later formal acceptance, rejection, rollback or correction records have precedence.

## Current task boundary remains unchanged

No implementation action was taken during Phase 1.

Current immediate gate is still:

```text
Undo only the immediately preceding Selected-Payment theme delta in the four theme files.
Do not roll back Spatial Flow Crypto Pay Trial V0.3.0.
Keep Project 2 Crypto Workspace OFF.
Validate the four restored theme files before starting BIG BATCH B runtime acceptance.
```

## Next inheritance phase

Continue full-body reading through:

```text
remaining Step 4E R5-E / S2 / N1 / N2 records
complete Step 4F Checkout lineage
complete Product Packaging lineage
complete Crypto BIG BATCH A/B lineage
latest Step 4F correction / ownership / handoff records
```

Only after that pass is complete may the repository-inheritance status be changed to COMPLETE.
