# Project 2 · Header / Footer visual-reference and prototype workflow START · 2026-09-13

## Trigger
The user clarified that text-only structural descriptions are not sufficiently visual for evaluating the Header / Footer redesign. The user explicitly requested a more visual workflow: study mature real-world websites, provide reference links and ask which directions they like, then synthesize the chosen traits into a Spatial Flow-specific visual template. The same method should later be used for Footer.

## Current project stage
Main-site Header production-quality / IA / interaction refinement remains active. No live source edit is authorized by this step.

## Workflow decision
Before implementation:
1. research a small curated set of mature websites whose header/footer systems are relevant to Spatial Flow's quiet editorial-commerce direction;
2. show the user direct links plus a concise explanation of which exact header/footer traits are worth inspecting;
3. ask the user which examples / traits they like or dislike;
4. create a standalone visual prototype/template for Spatial Flow based on the chosen direction;
5. keep the prototype outside the live WordPress source and repository unless/until the user approves it;
6. only after visual approval translate the approved design into a bounded implementation plan for current `header.php` / CSS / JS / menu ownership.

## Boundaries
- Static references remain useful, but Header/Footer are not being forced into literal 1:1 reproduction.
- Preserve WordPress menu ownership, WooCommerce bag/cart authority, Saved/Wishlist, Search, mobile menu, multisite Journal branch, and backend editability.
- Do not modify current live source yet.
- Do not perform the separate WordPress Page Inventory / Menu Admin Hygiene cleanup in this step; that remains a later dedicated audit.

## User acceptance status
Workflow requested/approved by user.

## Current stop point
Curate visual references and present them for user taste selection.

## Exact next action
Research and present mature header/footer reference candidates, then create a standalone Spatial Flow visual prototype after the user identifies preferred traits.
