# Step 4G — Home reference authority correction: strict 1:1 suspended

Date: 2026-09-19
Repository: `Th23144/spatial-flow-v2-preview-lab`

## User correction

During the initial Home current-vs-reference audit, the user explicitly stated that the canonical static Home reference itself is also unsatisfactory.

User statement:
`原稿的首页，我也不满意`

## Authority correction

Therefore `preview/spatial-flow-v1.html` is no longer an authoritative 1:1 target for Home.

It may still be used as:
- a source of editorial language;
- a source of useful section ideas;
- a comparison artifact.

It must NOT be used to force the production Home into a literal 1:1 reconstruction.

## Home task reclassification

Previous label:
`Home whole-page strict 1:1 re-audit`

New label:
`Home V2 product/visual redesign using current production truth + selected reference strengths`

The current live Home is also not accepted as final.

Therefore Home should be redesigned from first principles within Project 2 constraints, while preserving:
- real WordPress/WooCommerce ownership;
- backend editability;
- live product data;
- existing functional forms/links where retained;
- shared Header/Footer protected state.

## Immediate next step

Before source editing:
1. identify which current Home modules should be retained semantically;
2. identify which current modules should be removed or merged;
3. identify which reference ideas are worth keeping;
4. define the new Home information architecture and visual direction;
5. only after the user accepts that direction, map it to current dynamic owners and implement in bounded batches.

No production Home source should be modified until the new direction is approved.

Status:
HOME STRICT 1:1 SUSPENDED / HOME REDESIGN DIRECTION DEFINITION OPEN.
