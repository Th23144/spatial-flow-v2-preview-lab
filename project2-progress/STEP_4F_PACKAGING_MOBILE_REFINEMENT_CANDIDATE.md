# Step 4F · Product Packaging · Mobile Refinement Candidate

Date: 2026-08-08
Status: REJECTED BY USER · DO NOT USE AS FINAL MOBILE CONTRACT
Repository: `Th23144/spatial-flow-v2-preview-lab`

## Outcome

The user reviewed this first mobile refinement and explicitly rejected it:

```text
还不如没优化前的状态
```

The rejected direction was:

```text
- force Together / Separate / Custom into three compact columns on phone
- compress all three choices heavily
- keep the generated package editor as one shared area after the three choices
```

The palette correction itself was directionally useful, but the overall mobile interaction hierarchy was not accepted.

## Why it was rejected

The main problem was not simply spacing or color.

On phone, the mode selector and the generated package editor still felt visually detached:

```text
choose a grouping mode
↓
leave the selected option
↓
enter a separate package-editing block below all three options
```

The user requested a clearer ownership relationship:

> the package setup opened by a grouping choice should appear directly beneath that corresponding choice.

## Preserved requirements carried forward

```text
- desktop Compact Product Packaging contract remains accepted
- phone may use vertical grouping choices again
- selected-state palette should remain aligned to Spatial Flow ink / clay / paper
- generated package summaries remain compact
- one Package detail expands at a time
- Standard / Gift inside an expanded Package remain readable and compact
- Custom grouping remains package-centric
```

## Superseded by

```text
project2-progress/STEP_4F_PACKAGING_MOBILE_INLINE_MODE_CANDIDATE.md
```

The next candidate moves phone package setup directly below the currently selected grouping option.

## Real-site status

```text
Real SAFE5 Product Packaging implementation: NOT STARTED
Batch 6: PAUSED
Step 04: deferred
```

This rejected candidate must not be treated as an accepted visual contract.