# Wishlist V4 rejected — internal kill gate failure and research reset

Date: 2026-09-20
Repository: `Th23144/spatial-flow-v2-preview-lab`

## User verdict

User verdict on Wishlist V4:

> 很差，很一般

V4 is rejected.

## Root failure

The problem is not merely spacing, color, or interaction polish.

V4 still failed the user's long-term design standard:
- the composition is conceptually understandable but visually ordinary;
- the object-focus mechanism is not a sufficiently strong Design Hook;
- the page can too easily become a generic fashion/jewelry saved-list concept after changing the logo;
- it does not create the required quiet pressure / active whitespace / authored spatial logic;
- it is still an interface arrangement rather than a deeply considered object-space composition.

## Important process failure

The user's design standard explicitly requires:
- static visual admission before motion;
- internal KILL of weak concepts;
- research before implementation;
- references used for WHY IT WORKS rather than visual collage;
- do not show a merely "差不多" candidate.

V4 should have been killed internally before being shown.

## Reset

Do not create Wishlist V5 immediately.

Next step is research-only:
1. inspect the user's high-score reference corpus again;
2. define Wishlist-specific Page Role;
3. identify 2–3 reference mechanisms that actually solve Saved/Wishlist;
4. write a Wishlist-specific Design Grammar and Static Admission Gate;
5. only then build a new prototype if it passes internally.

## Hard rejection

Do not revive:
- V2 motion study;
- V3 Shop-grid derivative;
- V4 Private Index / Object Focus;
- old Home as visual authority.

Status:
WISHLIST V4 = REJECTED.
NEXT = RESEARCH / DESIGN GRAMMAR RESET.
